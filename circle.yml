machine:
  node:
    version: 8.4.0

dependencies:
  pre:
    - npm install -g npm@4.2.0
    - npm install -g eslint
    - npm install ytsearcher discordblacklist@2.0.0-rc https://github.com/wzhouwzhou/nodecpp-test
