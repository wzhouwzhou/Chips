machine:
  node:
    version: 8.4.0

dependencies:
  pre:
    - sudo apt-get install libcairo2-dev libjpeg8-dev libpango1.0-dev libgif-dev build-essential g++
    - npm install -g npm@4.2.0
    - npm install -g eslint
    - npm install ytsearcher discordblacklist
