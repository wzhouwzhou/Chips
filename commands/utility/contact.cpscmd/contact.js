module.exports = {
  name: 'contact',
  async func(msg, { send }) {
    /*    If (args[0]) {
      return client.channels.get('393857828126982144').send(`${author.tag} || ${channel.id}, ${channel + []}: ${suffix}`);
    }

    if (args[0].match(/r|reply/)) {
      let targetChannel = args[1];
      return client.channels.get(`${targetChannel}`).send(`${content.substring(content.indexOf(args[2]))}`);
    }
  },
}; */ 
    send('You can contact us in Chips\'s Official Server! `-invite`');
  },
};
